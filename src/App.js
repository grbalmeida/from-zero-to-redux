import React from 'react'
import { SearchBar, VideoList, VideoPlayer } from 'components'

const App = () => (
  <div>
    <SearchBar />
    <VideoList />
    <VideoPlayer />
  </div>
)

export default App
